import { defineConfig } from 'vite'

// https://vite.dev/config/
export default defineConfig({
    // server: {
    //     port: 3000, // Порт, на котором будет работать dev-сервер. По умолчанию 5173.
    //     open: true, // Автоматически открывать браузер при запуске сервера.
    //   },
    // build: {
    //     outDir: 'dist', // Директория, куда будет собираться проект. По умолчанию 'dist'.
    //     assetsDir: 'assets', // Подпапка для статических ресурсов (CSS, JS, изображения)
    //   },
})